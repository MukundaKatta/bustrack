{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile --filter ...@bustrack/web && pnpm prisma:generate",
  "buildCommand": "pnpm --filter @bustrack/web build",
  "outputDirectory": "apps/web/.next"
}

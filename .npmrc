# Expo / React Native needs a hoisted layout; Metro's resolver does not follow
# pnpm's default symlink scheme. See:
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
